# Domain docs

Single-context layout:

- `CONTEXT.md` at repo root — glossary / ubiquitous language only
- `docs/adr/` — architectural or hard-to-reverse design decisions when needed

Consumer rules: read `CONTEXT.md` before naming systems in specs or tickets. Do not put implementation detail in `CONTEXT.md`.
